<UserControl x:Class="mvvm.View.ClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mvvm.View"
             xmlns:viewModel="clr-namespace:mvvm.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:ClassViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="60"/>
            <ColumnDefinition Width="Auto" MinWidth="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock
            Text= "Overview over class"
            FontSize="18"
            FontWeight="SemiBold"
            Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"/>

        <TextBlock
            Text="Teacher:"
            FontSize="12"
            FontWeight="Bold"
            Grid.Row="1" Grid.Column="0"/>

        <ComboBox
            x:Name="comboTeacher"
            ItemsSource="{Binding Teachers}"
            SelectedItem="{Binding ClassBook.Teacher, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            IsHitTestVisible="{Binding IsChangeTeacher, UpdateSourceTrigger=PropertyChanged}"
            Focusable="{Binding IsChangeTeacher, UpdateSourceTrigger=PropertyChanged}"
            IsDropDownOpen="{Binding IsChangeTeacher, UpdateSourceTrigger=PropertyChanged}"
            Grid.Row="1" Grid.Column="1"/>

        <Button
            Content="{Binding ChangeCancelButtonText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
            Command="{Binding ChangeCancelCommand}"
            Grid.Row="1" Grid.Column="2" Width="70" HorizontalAlignment="Left"/>

        <DataGrid
            ItemsSource="{Binding ClassBook.Students}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            SelectionMode="Single"
            IsReadOnly="True"
            Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Name"
                    Binding="{Binding Name}"/>
                <DataGridTextColumn
                    Header="Birthday"
                    Binding="{Binding Birthday}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
